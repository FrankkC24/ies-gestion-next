/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,  // ❌ Temporalmente desactivar StrictMode
  compiler: {
    styledComponents: true,
  },
};

module.exports = nextConfig;
